import { CurrencyPtBrPipe } from './currency-pt-br.pipe';

describe('CurrencyPtBrPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyPtBrPipe();
    expect(pipe).toBeTruthy();
  });
});
